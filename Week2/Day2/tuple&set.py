#we cannot change the data inside a tuple. more used for dictionaries though
# n = ("apple", "banana", "orange")

#set - without specific order of elements. this is the least used 

colors = ["blue", "red", "yellow", "pink"]
new_colors = colors[1:2] #slicing 
print(colors)
print(new_colors)