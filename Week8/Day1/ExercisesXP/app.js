// 2.4. Create another file named app.js.
// 2.5. In app.js, import the array of person objects from the data.js module.
// 2.6. Write a function that calculates and prints the average age of all the persons in the array.
// 2.7. Use the imported array and the average age function in app.js.
// 2.8. Run app.js and confirm that the average age is correctly calculated and displayed.

// import { users } from './data.js';
// function averageAge() {
//     const usersAge = users.map(a => a.age);
//     const average = usersAge.reduce((a, b) => a + b, 0) / usersAge.length;
//     console.log(`Average age of users is ${average}.`)
// }

// averageAge()
